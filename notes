0. python3 -m venv .venv
1. source .venv/bin/activate    
pip install -r requirements.txt 
pip install flask flask-sqlalchemy python-dotenv    
pip freeze
pip freeze > requirements.txt

Load flow  :config-> model DB ->  Routes

10. 
1. Professional -> user id
    1. id -> name on UI
    2. two list display 
        2.1 today service -> id,  ALL-closed,date
        2.2 close servcice -> id, close

    --> 
